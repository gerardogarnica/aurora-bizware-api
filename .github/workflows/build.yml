name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  DOTNET_VERSION: "9.x"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore "Aurora Bizware.sln"

      - name: Build
        run: dotnet build "Aurora Bizware.sln" --configuration Release --no-restore

      - name: Test
        run: dotnet test "Aurora Bizware.sln" --configuration Release --no-restore --no-build

      - name: Publish
        run: dotnet publish "Aurora Bizware.sln" --configuration Release --no-restore --no-build
